{
  programs.gamemode = {
    enable = true;
    settings = {
      general = {
        desiredgov = "performance";
      };
    };
  };

  programs.dconf = {
    enable = true;
  };

  programs.virt-manager = {
    enable = true;
  };
}
